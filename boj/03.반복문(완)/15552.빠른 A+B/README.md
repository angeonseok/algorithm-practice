# 15552.빠른 A+B

## 문제 설명
본격적으로 for문 문제를 풀기 전에 주의해야 할 점이 있다. 입출력 방식이 느리면 여러 줄을 입력받거나 출력할 때 시간초과가 날 수 있다는 점이다.
Python을 사용하고 있다면, input 대신 sys.stdin.readline을 사용할 수 있다. 단, 이때는 맨 끝의 개행문자까지 같이 입력받기 때문에 문자열을 저장하고 싶을 경우 .rstrip()을 추가로 해 주는 것이 좋다.
또한 입력과 출력 스트림은 별개이므로, 테스트케이스를 전부 입력받아서 저장한 뒤 전부 출력할 필요는 없다. 테스트케이스를 하나 받은 뒤 하나 출력해도 된다.
자세한 설명 및 다른 언어의 경우는 이 글에 설명되어 있다.
이 블로그 글에서 BOJ의 기타 여러 가지 팁을 볼 수 있다.

## 입력
첫 줄에 테스트케이스의 개수 T가 주어진다. T는 최대 1,000,000이다. 다음 T줄에는 각각 두 정수 A와 B가 주어진다. A와 B는 1 이상, 1,000 이하이다.

## 출력
각 테스트케이스마다 A+B를 한 줄에 하나씩 순서대로 출력한다.

## 풀이 정리

### 핵심 아이디어
로직은 그냥 A+B인데 T가 최대 100만이라 input() 쓰면 터진다. sys.stdin.readline으로 갈아탄 문제.

### 시간복잡도
O(T). T가 100만이라 계산보다 입출력 속도가 전부다.

### 구현 포인트
- input()은 호출마다 부가 처리가 붙어서 느리다. sys.stdin.readline이 훨씬 빠름
- int()나 split()으로 감싸면 개행이 알아서 날아가서 rstrip 안 해도 됨. 문자열 그대로 쓸 때만 필요
- 출력은 print 그대로 뒀는데 통과함. 더 조여야 하면 모아서 한 번에 뿌리는 방법도 있다

### 실수했던 점

### 풀이 언어
Python
