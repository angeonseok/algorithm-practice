# 1753.최단경로

## 문제 설명
방향그래프가 주어지면 주어진 시작점에서 다른 모든 정점으로의 최단 경로를 구하는 프로그램을 작성하시오. 단, 모든 간선의 가중치는 10 이하의 자연수이다.

## 입력
첫째 줄에 정점의 개수 V와 간선의 개수 E가 주어진다. (1 ≤ V ≤ 20,000, 1 ≤ E ≤ 300,000) 모든 정점에는 1부터 V까지 번호가 매겨져 있다고 가정한다. 둘째 줄에는 시작 정점의 번호 K(1 ≤ K ≤ V)가 주어진다. 셋째 줄부터 E개의 줄에 걸쳐 각 간선을 나타내는 세 개의 정수 (u, v, w)가 순서대로 주어진다. 이는 u에서 v로 가는 가중치 w인 간선이 존재한다는 뜻이다. u와 v는 서로 다르며 w는 10 이하의 자연수이다. 서로 다른 두 정점 사이에 여러 개의 간선이 존재할 수도 있음에 유의한다.

## 출력
첫째 줄부터 V개의 줄에 걸쳐, i번째 줄에 i번 정점으로의 최단 경로의 경로값을 출력한다. 시작점 자신은 0으로 출력하고, 경로가 존재하지 않는 경우에는 INF를 출력하면 된다.

## 풀이 정리

### 핵심 아이디어
다익스트라 원형 그대로 짰다. 가중치가 전부 양수라 힙에서 제일 싼 놈부터 꺼내면 그 정점 거리는 그 자리에서 확정됨.

### 시간복잡도
O(E log V). V 2만 E 30만인데 결국 간선 수만큼 힙에 들어갔다 나오는 게 전부라 여유롭다.

### 구현 포인트
- 힙에서 꺼낸 d가 dist[u]랑 다르면 구버전이니까 걍 스킵. 이거 없으면 같은 정점 여러 번 펴게 됨
- 두 정점 사이에 간선이 여러 개여도 인접리스트에 (v, w) 다 박아두면 알아서 싼 쪽이 이김
- 못 가는 정점은 INF 문자열로. 한 줄씩 print 하지 말고 모아서 join으로 한 번에 뱉자

### 실수했던 점

### 풀이 언어
Python
