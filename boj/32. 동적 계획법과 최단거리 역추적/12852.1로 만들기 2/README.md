# 12852.1로 만들기 2

## 문제 설명
정수 X에 사용할 수 있는 연산은 다음과 같이 세 가지 이다.
1. X가 3으로 나누어 떨어지면, 3으로 나눈다.
2. X가 2로 나누어 떨어지면, 2로 나눈다.
3. 1을 뺀다.
정수 N이 주어졌을 때, 위와 같은 연산 세 개를 적절히 사용해서 1을 만들려고 한다. 연산을 사용하는 횟수의 최솟값을 출력하시오.

## 입력
첫째 줄에 1보다 크거나 같고, 106보다 작거나 같은 자연수 N이 주어진다.

## 출력
첫째 줄에 연산을 하는 횟수의 최솟값을 출력한다.
둘째 줄에는 N을 1로 만드는 방법에 포함되어 있는 수를 공백으로 구분해서 순서대로 출력한다. 정답이 여러 가지인 경우에는 아무거나 출력한다.

## 풀이 정리

### 핵심 아이디어
익숙한 1로 만들기 dp에 trace 배열 하나 더 얹은 거다. dp값 갱신될 때 어디서 왔는지 인덱스까지 같이 저장해두면 경로는 공짜로 나온다.

### 시간복잡도
O(N). 2부터 N까지 한 번 훑는 게 전부. 역추적은 나눠 내려가는 거라 훨씬 짧다.

### 구현 포인트
- 인덱스를 저장하면서 가야하기 때문에 min 안 쓰고 if로 하나씩 비교
- 먼저 dp[i-1] + 1로 깔아두고 2, 3으로 나눠떨어질 때만 더 작은지 확인
- while cur != 1에서 멈추니까 마지막에 1을 따로 append 해야 출력이 맞다
- ans는 N부터 쌓이는데 이 문제는 N부터 출력하는 게 맞아서 뒤집을 필요 없음

### 실수했던 점

### 풀이 언어
Python
