# 2050.알파벳을 숫자로 변환

## 문제 설명
알파벳으로 이루어진 문자열을 입력 받아 각 알파벳을 1부터 26까지의 숫자로 변환하여 출력하라.

## 제약사항
문자열의 최대 길이는 200이다.

## 입력
알파벳으로 이루어진 문자열이 주어진다.

## 출력
각 알파벳을 숫자로 변환한 결과값을 빈 칸을 두고 출력한다.

## 풀이 정리

### 핵심 아이디어
글자 하나씩 아스키 값에서 64 빼면 A가 1로 떨어진다.

### 시간복잡도
O(L). 글자 수만큼만 돌면 되고 길이 200 컷이라 널널.

### 구현 포인트
- 대문자 'A'가 65라 -64. 소문자 섞이면 이 식은 바로 깨진다
- py는 ord로 뽑고 cpp는 int 캐스팅해서 같은 계산
- py쪽은 input().strip() 걸어서 개행 딸려오는 거 막음

### 실수했던 점

### 풀이 언어
Python, C++
