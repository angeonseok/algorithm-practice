# 1984.중간 평균값 구하기

## 문제 설명
10개의 수를 입력 받아, 최대 수와 최소 수를 제외한 나머지의 평균값을 출력하는 프로그램을 작성하라.
(소수점 첫째 자리에서 반올림한 정수를 출력한다.)

## 제약사항
각 수는 0 이상 10000 이하의 정수이다.

## 입력
가장 첫 줄에는 테스트 케이스의 개수 T가 주어지고, 그 아래로 각 테스트 케이스가 주어진다.
각 테스트 케이스의 첫 번째 줄에는 10개의 수가 주어진다.

## 출력
출력의 각 줄은 '#t'로 시작하고, 공백을 한 칸 둔 다음 정답을 출력한다.
(t는 테스트 케이스의 번호를 의미하며 1부터 시작한다.)

## 풀이 정리

### 핵심 아이디어
10개 정렬해서 양 끝 하나씩 버리고 남은 8개 평균. 최대/최소만 빼면 되니 정렬 안 하고 sum - max - min으로도 같은 값이 나온다.

### 시간복잡도
O(T * 10). 수가 10개로 고정이라 정렬해봐야 상수.

### 구현 포인트
- 파이썬은 sum(arr) - max(arr) - min(arr)로 한 방에, C++은 sort 후 인덱스 1~8만 더함. 접근은 다른데 결과는 같다
- C++ round는 (double)로 캐스팅하고 넘겨야 함. 정수 나눗셈으로 먼저 잘리면 반올림할 게 없다
- 파이썬 round는 0.5에서 짝수 쪽으로 붙는 은행가 반올림임. 파이썬 반올림은 믿지말자, 애매하면 int(x + 0.5)로 직접 하는 게 안전
- 최댓값/최솟값이 중복이어도 딱 하나씩만 빼는 게 맞음

### 실수했던 점

### 풀이 언어
Python, C++
