# 3260.두 수의 덧셈

## 문제 설명
덧셈을 배운지 얼마 안 된 준환이는 덧셈에 아직도 어려움을 느낀다.
그래서 준환이는 N자리인 두 양수를 더하는 연습을 하기로 했다.
당신은 준환이를 위해 답안지를 만들어 주기로 했다.
두 양수가 주어질 때 두 수를 더한 결과를 구하는 프로그램을 작성하라.

## 입력
첫 번째 줄에 테스트 케이스의 수 T가 주어진다.
각 테스트 케이스마다 첫 번째 줄에 두 양의 정수 A, B(1 ≤ A, B< 10100)가 공백으로 구분되어 주어진다.
두 수는 0으로 시작되지 않는다.

## 출력
각 테스트 케이스마다 A+B값을 출력한다. 첫 번째 자리가 0으로 시작해서는 안 된다.

## 풀이 정리

### 핵심 아이디어
100자리짜리 큰 수 덧셈인데 파이썬이면 걍 int로 받아 더하면 그만. 자리올림 직접 짤 일 없음.

### 시간복잡도
O(T * D). D는 자릿수(100 이하). 큰 정수 덧셈이 자릿수에 비례.

### 구현 포인트
- C++로 갔으면 문자열 잡고 자리올림 손으로 짜야 하는 문제. 파이썬 무제한 정수가 다 해줌
- int로 더하면 앞자리 0이 붙을 일이 없어서 출력 조건도 자동으로 맞음
- 입력이 한 줄에 두 수라 map(int, input().split()) 한 방에 받아버림

### 실수했던 점

### 풀이 언어
Python
